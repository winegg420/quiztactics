// Rakip arama ekranı ADAYLARI (Ajan B doldurur). Sözleşme: ADAYLAR = [{ kod, baslik }] (kopyalama listesi için),
// varsayılan dışa aktarım bölüm bileşeni ({ secimler, onSec }).
export const ADAYLAR = [];
export default function AramaAdaylari() { return null; }
