// Altın isim ADAYLARI (Ajan A doldurur). Sözleşme: ADAYLAR = [{ kod, baslik }] (kopyalama listesi için),
// varsayılan dışa aktarım bölüm bileşeni ({ secimler, onSec }).
export const ADAYLAR = [];
export default function AltinIsimAdaylari() { return null; }
